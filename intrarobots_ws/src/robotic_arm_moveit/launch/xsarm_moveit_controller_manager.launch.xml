<launch>

  <!-- loads moveit_controller_manager on the parameter server -->
  <arg name="robot_model"                    default=""/>
  <arg name="fake_execution"                 default="false"/>
  <arg name="robot_controllers"              default=""/>
  <arg name="robot_fake_controllers"         default=""/>

 <group if="$(arg fake_execution)">

   <!-- Set the param that trajectory_execution_manager needs to find the controller plugin -->
   <param name="moveit_controller_manager" value="moveit_fake_controller_manager/MoveItFakeControllerManager"/>

   <!-- The rest of the params are specific to this plugin -->
   <rosparam file="$(arg robot_fake_controllers)"/>

 </group>

  <group unless="$(arg fake_execution)">

    <param name="moveit_controller_manager" value="moveit_simple_controller_manager/MoveItSimpleControllerManager"/>

    <!-- loads controllers to the param server -->
    <rosparam file="$(arg robot_controllers)"/>

  </group>

</launch>
